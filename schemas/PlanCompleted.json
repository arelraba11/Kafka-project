{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "PlanCompleted",
  "description": "Emitted by orchestrator.ts after all plan steps have produced results. Topic: conversation-events.",
  "type": "object",
  "required": ["conversationId", "timestamp", "eventType", "payload"],
  "properties": {
    "conversationId": {
      "type": "string",
      "description": "Unique identifier for the conversation."
    },
    "timestamp": {
      "type": "number",
      "description": "Unix epoch milliseconds at the time the event was produced."
    },
    "eventType": {
      "type": "string",
      "const": "PlanCompleted",
      "description": "Discriminator field identifying the event type."
    },
    "payload": {
      "type": "object",
      "required": ["results"],
      "properties": {
        "results": {
          "type": "array",
          "description": "Ordered array of tool result objects collected by the orchestrator, one per plan step.",
          "items": {
            "type": "object",
            "additionalProperties": true
          },
          "minItems": 1
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
