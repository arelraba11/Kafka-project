{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "PlanGenerated",
  "description": "Emitted by routerService.ts after the LLM generates an ordered list of tool steps. Topic: conversation-events.",
  "type": "object",
  "required": ["conversationId", "timestamp", "eventType", "payload"],
  "properties": {
    "conversationId": {
      "type": "string",
      "description": "Unique identifier for the conversation."
    },
    "timestamp": {
      "type": "number",
      "description": "Unix epoch milliseconds at the time the event was produced."
    },
    "eventType": {
      "type": "string",
      "const": "PlanGenerated",
      "description": "Discriminator field identifying the event type."
    },
    "payload": {
      "type": "object",
      "required": ["steps"],
      "properties": {
        "steps": {
          "type": "array",
          "description": "Ordered list of tool names the orchestrator must invoke sequentially.",
          "items": {
            "type": "string"
          },
          "minItems": 1
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
